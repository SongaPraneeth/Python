seems = []
# duplicates = []

# for i in values:
#   if i not in  seems:
#     seems.append[i]
#   else:
#     duplicates.append[i]
    
    
# duplicate_files = []
# for key,value in dic.items():
#   if value in duplicates:
#     duplicate_files.append(key)

# print(duplicate_files)